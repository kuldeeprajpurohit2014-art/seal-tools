[phases.setup]
nixPkgs = ["nodejs", "npm", "python3", "ffmpeg"]

[phases.install]
dependsOn = ["setup"]
cmds = [
  "ln -sf $(which python3) /usr/bin/python || true",
  "npm ci"
]

[phases.build]
dependsOn = ["install"]
cmds = ["npm run build"]
